# V.35 CPLD 时序项目：重要问题与答案复习

> 用途：复习本项目的设计意图、异步输入处理、相位测量、边沿判决、状态机边界与系统局限。
> 本文不是逐字聊天记录，而是将已经讨论过的核心问题合并、纠正并整理成可用于面试表达的答案。
> 当前版本以已经完成的讨论为主；寄存器接口、综合约束、完整验证和系统硅后问题将在后续问答后继续补充。

## 一、项目目标与架构

### 1. 原始问题是什么？

V.35 接口在约 $20\times64\ \mathrm{kHz}$ 的接收时钟下可能出现误码。一个可能原因是接收时钟上升沿与数据跳变过于接近，导致接收端建立时间或保持时间不足，而在相邻频率下二者的相对相位可能不同。

本项目不是直接证明物理层建立/保持时间违例，而是用 50 MHz 参考时钟，把同步后的 RCLK 上升沿与 DATA 跳变之间的时间离散成“参考时钟拍数”，为 CPU 选择上升沿或下降沿采样提供可观测数据。

### 2. 为什么不能只看 20×64 kHz 出错就断定是建立/保持时间问题？

因为相同症状也可能来自占空比异常、时钟或数据抖动、板级信号完整性、软件配置、同步量化、接口器件差异等因素。相位测量只能支持或削弱某个假设，不能单独完成因果证明。最终仍需把相位数据与真实误码、示波器观测和环境条件关联起来。

### 3. 当前架构包含哪些主要模块？

- 两级输入同步器：分别同步 RCLK 和 DATA。
- 事件检测器：从同步后的电平产生 `rclk_rise_evt` 和 `data_toggle_evt`。
- 相位测量与控制状态机：等待 RCLK、等待 DATA、保存结果并处理超时。
- 寄存器接口：由 CPU 启动测量、读取结果和状态。
- 扩展版可增加连续采样、统计计数和边沿判决支持，但复杂统计更适合放在 CPU。

### 4. CPLD 与 CPU 应如何分工？

CPLD 负责必须逐拍完成且与硬件时序紧密相关的工作：同步输入、检测事件、测量拍数、记录异常和提供原子结果。CPU 负责需要多样本和策略调整的工作：统计分布、判断边沿、设置滞回、记录历史、执行回滚和关联误码。

这样既避免在 CPLD 中堆积复杂统计逻辑，也保留软件策略的可修改性。

## 二、异步输入与事件检测

### 5. 为什么 RCLK 和 DATA 不能直接进入状态机？

它们相对于 50 MHz 参考时钟是异步信号。若直接使用，采样触发器可能进入亚稳态，而且不同逻辑可能在同一拍看到不一致的值，破坏状态机、计数器和事件检测的确定性。

### 6. 两级同步器解决了什么？

第一级触发器可能进入亚稳态；第二级在下一拍再次采样，使亚稳态传播到普通逻辑的概率显著降低。它降低了亚稳态失效概率，并把异步信号转换到参考时钟域中使用。

### 7. 两级同步器没有解决什么？

它不能消除亚稳态，只能降低其传播概率；不能保证捕获窄于参考时钟周期的脉冲；不能保留异步边沿的精确模拟时间；也不能保证分别同步的 RCLK 与 DATA 保持原来的精确相对相位。

### 8. 为什么 RCLK 和 DATA 分别同步后，相位测量存在量化误差？

两个物理边沿落在 50 MHz 采样边界附近时，可能被归入相邻的参考时钟桶。每条同步链还可能因亚稳态解析而多延迟一拍。因此测得的是“同步后事件之间的离散拍数”，不是连续时间中的精确相位，边界附近可能出现约一拍的不确定性。

### 9. 为什么事件检测器要放在同步器之后？

边沿检测通常比较当前拍与上一拍的电平。如果直接对异步输入做比较，亚稳态和组合毛刺可能产生虚假事件或丢失事件。同步后再检测，事件才属于统一参考时钟域，可以被状态机可靠消费。

### 10. `data_toggle_evt` 为什么既要检测上升沿也要检测下降沿？

本项目关注 DATA 何时发生变化，而不是只关注某个逻辑方向。可用当前同步值与上一拍同步值异或得到任意方向的跳变事件。若后续发现上升、下降转换具有不同分布，则应分别统计，而不能混为一个分布。

### 11. 启动后为什么需要 `armed` 或首样本屏蔽？

事件检测依赖“当前值与历史值”的比较。复位或启动后历史寄存器尚未代表一个有效的前一拍，初始差异可能被误报为真实边沿。`armed` 表示历史样本已经建立，之后的差异才允许生成事件。

## 三、相位、周期与计数器

### 12. 50 MHz 参考时钟的分辨率是多少？

参考周期为：

$$
T_{ref}=\frac{1}{50\ \mathrm{MHz}}=20\ \mathrm{ns}
$$

所以相位结果以 20 ns 为一个离散拍。它只能说明事件落入哪个采样桶，不能提供亚拍级顺序。

### 13. 19×、20×、21×64 kHz 的 RCLK 周期约为多少个参考拍？

频率分别约为 1.216、1.280、1.344 MHz，对应周期约为：

- 19×：$41.1$ 个 50 MHz 参考拍；
- 20×：$39.1$ 拍；
- 21×：$37.2$ 拍。

这些数值不是整数，因此同步后观测到的周期拍数会在相邻整数间变化。

### 14. 为什么计数器位宽不能只按照一个 RCLK 周期设计？

若 DATA 长期不跳变，等待时间可能跨越很多个 RCLK 周期；若 RCLK 消失，等待时间甚至没有周期上界。因此计数器位宽必须由明确的超时上限决定，而不是只由正常周期决定。当前设计采用至少 10 位，是为了覆盖计划中的参考拍超时范围，并需配合饱和或终止机制。

### 15. 计数器为什么不应静默回绕？

回绕会把一次很长的等待伪装成很小的相位，CPU 可能将异常误判为安全样本。达到最大值时应饱和、置位溢出/超时状态，或直接结束测量。

### 16. `phase_count` 的结果语义应如何定义？

必须在规格中说明它表示“自最近一次已检测 RCLK 上升沿之后经过的完整参考拍数”，还是“当前计数寄存器的编码”。从 RCLK 事件拍记为 0 最直观：同拍 DATA 跳变结果为 0，下一拍跳变结果为 1。测试平台必须使用同一定义，否则容易出现差一拍错误。

### 17. 阈值比较为什么常出现 `N` 与 `N-1` 的错误？

时序逻辑中的比较使用的是更新前寄存器值。若计数器从 0 开始且当前拍之后将递增，想在第 $N$ 拍触发，往往应比较当前值是否为 $N-1$。不能机械套用，必须先画出逐拍表，明确事件拍是否计入。

## 四、测量状态机与超时

### 18. 基本状态机可以如何划分？

- `IDLE`：等待启动。
- `WAIT_RCLK`：等待一个同步后的 RCLK 上升沿作为起点。
- `WAIT_DATA`：测量 DATA 跳变相对 RCLK 的离散相位。
- `DONE`：保持结果与状态，等待 CPU 读取、清除或再次启动。

状态名称不是重点，重点是每个状态对事件、计数器、结果有效性和超时的语义明确。

### 19. 为什么要分别处理“无 RCLK”和“无 DATA”超时？

两者说明不同故障：无 RCLK 表示接收时钟停止或未被捕获；有 RCLK 但 DATA 多周期不跳变可能只是数据保持不变，也可能是接口异常。若只用一个超时位，CPU 无法区分诊断路径。

### 20. 在 `WAIT_DATA` 中又检测到新 RCLK 上升沿，应怎样处理？

当前选择是：清零 `phase_count`，以最新 RCLK 作为起点。这样结果始终表示 DATA 跳变距离最近一次 RCLK 上升沿多少拍，而不是距离最初启动时的 RCLK 多久。

这是一种自洽的周期内相位语义，但必须另设机制防止 DATA 永不跳变时无限等待。

### 21. 为什么需要不止一个计数器？

- `phase_count`：按 50 MHz 参考拍计数，每次新 RCLK 上升沿清零，用于周期内相位。
- `no_data_cycle_count`：仅在新 RCLK 上升沿到来且尚无 DATA 时递增，用于判断连续多少个 RCLK 周期没有 DATA 跳变。
- `no_rclk_count`：按参考拍递增，每次 RCLK 上升沿清零，用于检测 RCLK 消失。

若只有 `no_data_cycle_count`，RCLK 停止时它也不会再增加，状态机仍可能永久等待。

### 22. DATA 长期保持不变是否一定是错误？

不一定。真实数据流可能允许长串相同位，因此“多个周期无跳变”首先是“本次无法取得相位样本”，不应未经协议约束就直接等同于链路故障。硬件应报告 `no_data_timeout`，由系统结合协议和业务状态解释。

### 23. `data_toggle_evt` 与 `rclk_rise_evt` 同拍时，结果应保存旧计数还是写 0？

两种定义都能实现，但语义不同：

- 保存旧 `phase_count`：把 DATA 归入刚结束的旧 RCLK 周期。
- 写 0：把 DATA 归入刚开始的新 RCLK 周期。

当前规格选择写 0，因为结果定义为相对于“最近一次已检测到的 RCLK 上升沿”。但 0 只表示两个同步事件落在同一个 50 MHz 观测桶，不能证明物理边沿真正同时发生，也不能证明 DATA 位于 RCLK 之后。

### 24. DATA、RCLK 与无 DATA 超时同拍时，应如何排序？

应显式规定优先级，不能依赖非阻塞赋值的书写顺序。推荐：

1. 有效 DATA 跳变；
2. 超时；
3. 普通 RCLK 更新；
4. 普通计数。

因此 DATA 与 RCLK 同拍且恰逢超时阈值时，仍输出有效相位 0；因为目标事件已经在截止拍被观察到，不应再把本次测量报告为“未观察到 DATA”。

### 25. 为什么同拍优先级必须写进规格与验证计划？

同一组 RTL 可以因 `if/else` 顺序不同而产生不同合法实现。如果规格没有规定，Designer、DV 和软件可能各自假定不同语义，最后出现仿真“都合理”但系统解释冲突。关键同拍组合必须作为定向测试和断言覆盖。

## 五、多样本统计与环形相位

### 26. 为什么不能简单对相位拍数做算术平均？

相位是环形量。若一个周期约 40 拍，39 拍和 1 拍在圆周上非常接近，算术平均却会得到约 20 拍，制造一个几乎没有样本的“中心”。应使用环形统计、直方图或先选择正确的周期展开点。

### 27. 单峰与双峰分布有什么不同？

单峰通常说明事件集中在一个稳定相位附近；双峰可能来自两类真实 DATA 边沿、跨周期折返、抖动模式、同步量化或系统工作状态混合。双峰数据不能强行压缩成一个均值，应先查明是否需要按边沿类型或状态分组。

### 28. 为什么只记录平均相位不够？

平均值会掩盖尾部危险样本、双峰分布和异常比例。至少应保留样本数、分布或直方图、典型裕量、低分位裕量、绝对最小值、危险样本数以及这些样本是否跨窗口重复出现。

### 29. “最优边沿”应该如何定义？

不能只看哪个边沿的平均距离更大。更合理的目标是：在考虑测量量化、抖动、建立/保持要求和尾部风险后，哪个候选边沿具有更高且更稳定的最小安全裕量。同时还要检查它是否达到绝对安全门槛。

### 30. 为什么“相对更优”不等于“安全”？

例如上升沿裕量 1 拍、下降沿裕量 2 拍。下降沿虽然相对多 1 拍，但差异可能来自量化或抖动，而且 2 拍本身也可能不足以覆盖实际不确定性。因此应报告 `no_safe_edge` 或降级状态，而不能把下降沿宣称为安全。

## 六、边沿切换、滞回与风险

### 31. 为什么系统不应每个窗口都追随局部最优边沿？

若窗口 1 是上升沿 4 拍、下降沿 5 拍，窗口 2 又变成上升沿 5 拍、下降沿 4 拍，1 拍差异可能只是量化或抖动。逐窗口切换会产生抖振，还会引入切换瞬间的漏采、重复采样或链路扰动。

### 32. 如何设计改善门槛？

只有候选边沿比当前边沿至少好 $H$ 拍，并且连续多个窗口成立，才进行常规切换。例如：

$$
M_{new}-M_{current}\ge 3\ \text{拍}
$$

具体的 3 拍不是固定真理，应由参考时钟分辨率、同步不确定性、抖动和误码实验标定。

### 33. 改善门槛与危险门槛有什么区别？

- 改善门槛回答：“新边沿是否明显优于当前边沿？”
- 危险门槛回答：“当前边沿是否已经低于最低可接受裕量？”

常规切换需要显著且持续的改善；紧急切换可在当前裕量低于危险阈值时触发，但仍必须确认另一边沿确实达到可接受裕量。若两边都危险，应报告无安全边沿，而不是在两个坏选择间反复切换。

### 34. 除门槛外，为什么还需要连续窗口与滞回？

改善门槛抑制幅度很小的变化，连续窗口抑制一次性波动，滞回则让切入与切出条件不同，避免在阈值附近来回翻转。三者解决的问题不同，不能只设一个阈值就认为策略稳定。

### 35. 边沿切换为什么还需要安全执行边界？

即使决策正确，任意时刻修改采样沿也可能造成当前字符或当前位漏采、重复采样或短暂错误。切换应发生在协议允许的空闲期、帧边界、复位阶段或其它明确定义的安全点，并保留旧配置和回滚机制。

## 七、异常值与尾部风险

### 36. 15 个样本为 8 拍，1 个样本为 1 拍，是否应直接丢弃 1 拍样本？

不应仅凭 15:1 就直接丢弃。它可能是同步量化、亚稳态延迟或测量毛刺，也可能是真实但低概率的危险跳变。正确做法是将其标为可疑样本，检查伴随状态并扩大观察窗口，而不是立即让它支配切换，也不是静默删除。

### 37. 完全相信单个最差样本有什么风险？

单个假异常可能让系统过度反应：不必要地切换边沿、误报 `no_safe_edge`、进入降级状态，甚至切换到实际上更危险的边沿。最小值只描述最坏观测，不代表整体分布。

### 38. 直接删除单个最差样本有什么风险？

它可能掩盖真实的低概率误码机制。在长时间运行或低目标误码率系统中，低概率事件仍可能不可接受。异常样本必须留下计数和上下文，后续通过重复性、跨窗口出现和外部误码观测判断其真实性。

### 39. 1000 个样本中 990 个为 8 拍、10 个为 1 拍，而且分散跨窗口出现，还能视为异常吗？

不能再轻易视为孤立异常。它们重复且跨时间窗口出现，说明存在约 1% 的稳定尾部风险，必须调查其触发条件和误码相关性。

### 40. 平均值、最小值和第 5 百分位分别会掩盖什么？

- 平均值约为 7.93 拍，会把 1% 的危险尾部稀释掉。
- 最小值为 1 拍，会让少数样本完全支配结论，忽略 99% 主体分布提供的信息。
- 第 5 百分位可能约为 8 拍，因为危险样本仅占 1%，仍可能完全跨过尾部。

因此不存在单一万能统计量。应同时报告主体分布和尾部风险。

### 41. 一个更稳健的统计方案是什么？

常规边沿判决可使用低分位数或直方图，降低单个假异常的支配作用；风险监控同时保留绝对最小值、危险样本计数、危险样本率及跨窗口重复情况。若危险样本持续出现，则升级为 `unstable` 或 `no_safe_edge`，并结合真实误码验证。

### 42. 为什么 16 个样本通常不足以证明安全？

没有观察到某事件，只能说明它在这 16 次中没有出现，不能证明其概率低于系统要求。若目标误码率很低，样本规模必须与要排除的风险概率匹配，还要覆盖频率、温度、电压、板卡和线缆差异。

## 八、方案能力边界

### 43. 相位 0 是否等于建立/保持时间违例？

不等于。相位 0 只说明同步后的两个事件落入同一个 20 ns 观测桶。物理上 DATA 可能在 RCLK 前，也可能在后，二者还可能相隔接近一个参考周期。它是高风险提示，不是模拟时序违例的直接证明。

### 44. 提高参考时钟是否一定更好？

提高参考时钟能改善时间分辨率，但会增加 CPLD 时序收敛压力、功耗和计数速率，也不能消除亚稳态。若目标器件、布局布线和接口要求不允许，盲目提频可能使实现本身不可靠。

### 45. 如果上升沿和下降沿都没有足够裕量怎么办？

系统应报告 `no_safe_edge`，保持安全默认值或进入明确的降级模式，并结合误码监测、重试或链路复位。长期来看需要更多采样相位、可编程延迟、多相时钟、延迟链或接口硬件修改，而不是继续优化二选一算法。

### 46. 什么时候应停止改 CPLD 算法，转向硬件或接口架构修改？

当测量分辨率不足以区分风险、两个候选边沿都不安全、问题强烈依赖信号完整性或环境、算法指标改善却不能降低真实误码，或所需功能超出 CPLD 的时序和资源能力时，应转向板级、接口器件、时钟结构或采样架构修改。

## 九、常见表达错误与修正

### 47. “另一边沿多 1 拍，所以应该切换”错在哪里？

它只比较相对值，没有考虑一拍量化误差、结果稳定性、绝对安全门槛和切换代价。正确表述是：只有改善明显、持续存在、候选边沿达到绝对安全裕量，并能在安全边界执行时，才切换。

### 48. “小于 1 拍的时候再切”为什么含义不清？

必须说明是当前边沿的绝对裕量小于 1 拍，还是候选边沿相对改善小于/大于 1 拍。前者是危险门槛，后者是改善门槛；两个门槛不能混为一句话。

### 49. “最小值掩盖正常值”怎样说更准确？

更准确的说法是：最小值不会隐藏正常样本，但会忽略整体分布，让少量最差样本完全支配判决。它适合风险报警，却不适合作为唯一的典型状态描述。

### 50. “两级同步器保证信号安全”为什么不严谨？

同步器只把亚稳态传播概率降低到工程上可接受的水平，前提还包括正确的器件参数、时钟频率、布局和约束。它不能保证绝对安全，也不能自动解决脉冲丢失、多位一致性或相对相位精度问题。

## 十、目前尚待继续训练的重点

以下内容尚未形成完整问答，后续应继续补入本文：

1. 状态机收尾：重复 `start`、`DONE` 保持、清除与新结果同拍冲突。
2. CDC 深挖：窄脉冲捕获、复位释放、同步器属性和约束。
3. 计数器波形题：逐拍结果、阈值边界、饱和和频率异常。
4. 寄存器接口：原子快照、读清除、写一触发、旧结果与新结果区分。
5. 可综合 RTL：非阻塞赋值、组合默认值、多驱动、参数化和锁存器。
6. Designer 视角验证：定向用例、断言、参考模型和覆盖率。
7. 综合与时序约束：时钟定义、异步路径、同步器识别、关键路径与资源。
8. 系统/硅后：实验设计、相位与误码关联、温压与板卡差异、回滚和降级。
9. DV 专项：需求—测试—覆盖映射、随机激励、容差模型和缺陷归因。
10. 最终混合答辩：两分钟项目介绍、方案替代、危险假设、已验证与未验证结论。

## 十一、一分钟总复习

本项目用 50 MHz 时钟对异步 RCLK 和 DATA 分别进行两级同步，再检测 RCLK 上升沿和 DATA 任意跳变，以参考拍数表示二者同步后的离散相位。该结果存在 20 ns 量化和同步链不确定性，不能直接等同于物理建立/保持裕量。状态机必须明确新 RCLK、DATA、超时同拍时的优先级，并分别处理无 RCLK 和多周期无 DATA。CPU 不能只看平均值或单窗口最优，而应同时分析主体分布、尾部风险、绝对安全裕量，并用改善门槛、连续窗口、滞回和安全切换边界避免抖振。最终是否解决问题必须由真实误码闭环验证；若两个采样沿都不安全，就需要更多相位档位或修改硬件架构。
